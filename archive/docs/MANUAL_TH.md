# คู่มือการใช้งานระบบ NeuroTrader-Hybrid-Agent (Ultimate Edition)

ยินดีต้อนรับสู่ **NeuroTrader Ultimate** ระบบเทรดอัจฉริยะที่ผสานพลังระหว่าง Python AI และความเร็วแบบ Rust-style architecture มาพร้อมระบบ Dashboard และการแจ้งเตือนผ่าน Discord

---

## 1. ฟีเจอร์ใหม่ (Ultimate Features) 🚀

- **Shadow Mode (โหมดเงา)**: ระบบจะ "เทรดทิพย์" โดยบันทึกข้อมูลลง Database แต่ไม่ส่งคำสั่งซื้อขายจริงไปยัง Broker ทำให้คุณทดสอบกลยุทธ์กับกราฟจริงได้ 100% โดยไม่เสี่ยง
- **Discord Notification**: เปลี่ยนการแจ้งเตือนจาก Telegram เป็น Discord ที่รวดเร็วและสวยงามกว่า
- **Live Dashboard**: หน้าเว็บสำหรับดูสถานะบอท กราฟราคา และประวัติการเทรดแบบ Real-time
- **Structured Database**: เก็บประวัติการเทรดทั้งหมดลง `sqlite` ทำให้วิเคราะห์ย้อนหลังได้แม่นยำ

---

## 2. การติดตั้ง (Installation)

### 2.1 เตรียมสภาพแวดล้อม
```bash
cd /home/pop/.gemini/antigravity/scratch/NeuroTrader
python3 -m venv venv
source venv/bin/activate  # หรือ venv\Scripts\activate บน Windows
```

### 2.2 ติดตั้งโปรแกรม
```bash
pip install -r requirements.txt
```
> **หมายเหตุ**: หากรันบน Linux และต้องการเชื่อมต่อ MT5 จริง คุณอาจต้องตั้งค่า Wine หรือใช้ Windows VM แต่ถ้าไม่มี ระบบจะทำงานใน **Mock Mode** (จำลองเทรด) ให้โดยอัตโนมัติ

---

## 3. การตั้งค่า (Configuration)

### 3.1 ตั้งค่าหลัก (`config/main_config.yaml`)
```yaml
system:
  shadow_mode: true        # [แนะนำ] เปิด True ไว้เพื่อเทรดแบบ Shadow ก่อน
  mode: "PAPER_TRADE"
  
stealth:
  enable_jitter: true      # เปิดระบบหน่วงเวลาสุ่มป้องกันการดักจับ
```

### 3.2 ตั้งค่าความลับ (`config/secrets.yaml`)
สร้างไฟล์ใหม่ หรือแก้ไฟล์เดิม:
```yaml
# Discord Config (ของใหม่!)
discord_token: "ใส่_DISCORD_BOT_TOKEN_ที่นี่"
discord_channel_id: "ใส่_CHANNEL_ID_ตัวเลข_ที่นี่"

# LLM & MT5 Config
openai_api_key: "sk-..."
mt5_login: 123456
mt5_password: "..."
mt5_server: "MetaQuotes-Demo"
```

### 3.3 ตั้งค่า Local LLM (Ollama) - 🧠 สมองใหม่
เพื่อให้บอทคิดวิเคราะห์ข่าวได้โดยไม่ต้องเสียเงิน API:
1.  **ติดตั้ง Ollama**: รันคำสั่ง `curl -fsSL https://ollama.com/install.sh | sh`
2.  **ดึง Model**: รันคำสั่ง `ollama pull mistral` (หรือ `llama3` ตามชอบ)
3.  **ตรวจสอบ**: ลองรัน `ollama run mistral` ดูว่าคุยได้ไหม ถ้าได้คือกดออก (Ctrl+d) แล้วจบ
4.  **Config**: ในไฟล์ `config/main_config.yaml` ตรวจสอบว่าตั้งค่าเป็น:
    ```yaml
    llm:
      provider: "ollama"
      model_name: "mistral"
      low_vram: true  # [New] เปิด True หาก GPU VRAM น้อย (<8GB)
    ```

### 3.4 🐧 การติดตั้งบน Linux (Advanced)
หากคุณใช้ Linux สามารถรันบอทเชื่อมต่อ MT5 ได้ผ่าน **Wine**:

1. **ติดตั้ง Environment ด่วน**:
   ```bash
   bash tools/setup_wine_env.sh
   bash tools/wine_runner.sh -m pip install -r requirements.txt
   ```
   *(สคริปต์นี้จะช่วยลง Python และ pip ใน Wine ให้เอง)*

2. **การรันคำสั่งต่างๆ**:
   ให้ใช้ `bash tools/wine_runner.sh` นำหน้าคำสั่ง python ปกติ เช่น:
   ```bash
   bash tools/wine_runner.sh tools/fetch_data.py
   ```

---

## 4. วิธีการใช้งาน (Usage)

### 🔴 รันบอทเทรด (The Core)
```bash
python3 src/main.py
```
เมื่อรันสำเร็จ บอทจะแจ้งเตือนไปที่ Discord ว่า "🚀 NeuroTrader Ultimate Started."

### 📊 เปิดหน้า Dashboard (The View)
เปิด Terminal อีกช่อง (New Terminal) แล้วรัน:
```bash
source venv/bin/activate
streamlit run tools/dashboard.py
```
ระบบจะเปิด Browser ขึ้นมาแสดงหน้า Dashboard ที่: `http://localhost:8501`
- **Total Trades**: จำนวนการเทรดทั้งหมด
- **Real vs Shadow**: แยกแยะให้เห็นชัดเจนว่าอันไหนเทรดจริง อันไหนเทรดเงา
- **Charts**: กราฟแสดงจุดเข้าซื้อขายบน Timeframe จริง

---

## 5. การปรับจูน (Tuning Guide)

### อยากเปลี่ยน Logic การเทรด?
ไปที่: `src/brain/rl_agent.py`
```python
# ตัวอย่าง Logic ง่ายๆ
if settings['rsi'] > 70:
    return "SELL"
elif settings['rsi'] < 30:
    return "BUY"
```

### อยากดู Database โดยตรง?
ใช้โปรแกรม **DB Browser for SQLite** เปิดไฟล์:
`data/memory/neurotrader.db`
คุณจะเห็นตาราง `trade_history` ที่เก็บทุกอย่างไว้อย่างละเอียด

---

## 6. การแก้ปัญหา (Troubleshooting)

- **Discord ไม่แจ้งเตือน**: 
  - เช็ค `discord_token` และ `discord_channel_id` (ต้องเป็นตัวเลขล้วน)
  - ต้อง Invite บอทเข้า Server ก่อน และบอทต้องมีสิทธิ์ "Send Messages"
  
- **Dashboard ไม่ขึ้นข้อมูล**: 
  - ต้องรัน `src/main.py` ก่อนอย่างน้อย 1 ครั้งเพื่อให้สร้างไฟล์ Database (`neurotrader.db`)

- **ต้องการรันยาวๆ แบบไม่เฝ้าจอ**:
  ใช้ Watchdog: `python3 tools/watchdog.py`

---
*ขอให้สนุกกับระบบเทรดอัจฉริยะของคุณครับ!* 💵
